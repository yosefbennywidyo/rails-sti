<div id="<%= dom_id team %>">
  <div class="col  mb-2">
    <div class="card" style="width: 18rem;">
      <div class="card-body">
        <h5 class="card-title"><%= team.name %></h5>
        <p class="card-text"><%= team.email %></p>
        <a href="<%= client_path(team) %>" class="btn btn-primary">Detail</a>
      </div>
    </div>
  </div>
</div>
