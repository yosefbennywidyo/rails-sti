<div id="<%= dom_id stock %>">
  <div class="col  mb-2">
    <div class="card" style="width: 18rem;">
      <div class="card-body">
        <h5 class="card-title"><%= stock.name %></h5>
        <p class="card-text"><%= stock.email %></p>
        <a href="<%= client_path(stock) %>" class="btn btn-primary">Detail</a>
      </div>
    </div>
  </div>
</div>
